// import classes from './farm-single-box.module.css';

// function FarmSingleBox(props) {
//   const boxName = `box${props.boxNum}`;
//   const isOpen = props.isOpen;
//   return (
//     <div
//       className={boxName + ' ' + isOpen ? 'open' : 'occupied'}
//       onClick={props.onClick}
//     ></div>
//   );
// }

// export default FarmSingleBox;
